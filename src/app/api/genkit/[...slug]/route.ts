// This file can be deleted as Genkit API routes are no longer needed.
// If you plan to use Genkit for other features later, you can keep it.
// For now, its content is removed to reflect the removal of the parseTaskFlow.

// import { genkitNextHandler } from '@genkit-ai/next';
// import '@/ai/dev'; // Ensure your flows are imported

// export const { GET, POST } = genkitNextHandler();
